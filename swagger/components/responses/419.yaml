description: Token CSRF bị thiếu, không hợp lệ hoặc đã hết hạn
content:
  application/json:
    schema:
      properties:
        message:
          title: Thông báo ngắn
          type: string
          description: Token CSRF không khớp.
          example: CSRF token mismatch.
      type: object